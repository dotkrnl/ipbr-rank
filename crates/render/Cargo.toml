[package]
name = "ipbr-rank-render"
version.workspace = true
edition.workspace = true

[lib]
name = "ipbr_render"

[dependencies]
ipbr-core = { package = "ipbr-rank-core", path = "../core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
